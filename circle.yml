machine:
  timezone:
    America/New_York

  php:
    version: 7.0.4

  environment:
    PATH: "${PATH}:${HOME}/terminus/bin


dependencies:
  cache_directories:
    - ~/terminus

  pre:


    # Install Terminus
    - ./bin/install_terminus.sh

 
